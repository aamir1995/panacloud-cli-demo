scalar AWSDate
scalar AWSTime
scalar AWSDateTime
scalar AWSTimestamp
scalar AWSEmail
scalar AWSJSON
scalar AWSURL
scalar AWSPhone
scalar AWSIPAddress

type Student {
  id: String!
  name: String
}

type Query {
  student (id: String): Student

}

type Mutation {
  addStudent (name: String!): Student!
}
